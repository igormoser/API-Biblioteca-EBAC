services:
  app:
    build: .

    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - LOGIN=admin
      - PASSWORD=admin
      - DATABASE_URL=sqlite:///./biblioteca.db
      - PYTHONUNBUFFERED=1
    command: ["poetry", "run", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]